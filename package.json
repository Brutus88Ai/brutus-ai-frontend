{
  "name": "brutus-ai-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm install && npm run build && mv src/dist dist",
    "preview": "cd src && npm run preview"
  }
}

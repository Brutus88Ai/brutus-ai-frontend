{
  "name": "brutus-ai-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm install && npm run build && cp -r dist ../dist",
    "preview": "cd src && npm run preview"
  }
}
